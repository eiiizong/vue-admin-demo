import type { App } from 'vue'
// import { setupPermissionDirective } from './permission'
// import { setupLoadingDirective } from './loading'

/**
 * 注册全局指令
 */
const setupGlobDirectives = (app: App) => {
  console.log(app)
}

export { setupGlobDirectives }
